extern int printf(char[] format, ...);

int main() {
    int j = 0;
    for (int i = 2 to 10) {
        j = i;
        printf("%d\n", i);
        if (i == 5) {
            break;
        }
    }
    printf("%d\n", j);

    return 0;
}