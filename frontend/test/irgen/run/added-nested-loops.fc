extern int printf(char[] format, ...);

int main() {
    for (int i = 0 to 10) {
        for (int j = 0 to 10) {
            if (i < 8) {
                break;
            }

            int k = i + j;
            while (k < 15) {
                if (k == i || k == j || k % 2 == 0){
                    k += 2;
                    continue;
                }
                printf("%d \n", k);
                k += 1;
            }
        }
    }
    return 0;
}