#include <cstdlib.fh>

int main(int argc, char[][] argv) {
    if (argc != 2) {
        printf("Usage: %s num\n", argv[0]);
        exit(1);
    }
    int num = atoi(argv[1]);
    printf("num: %d\n", num);
    for (int i = 0 to num) {
        printf("%d\n", num - i);
    }
    return 0;
}
